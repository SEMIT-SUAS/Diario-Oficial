# =====================================================
# DOM - Configurações do PostgreSQL
# =====================================================

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=root
DB_NAME=dom_database

# Connection Pool
DB_POOL_MIN=2
DB_POOL_MAX=10

# Application
NODE_ENV=development
PORT=3000

# JWT Secret (mude em produção!)
JWT_SECRET=seu-secret-super-seguro-aqui-mude-em-producao

# Cloudflare R2 (opcional - para upload de arquivos)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=dom-uploads
